<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">

        <!--
          Maps SDK for Android kräver nyckeln direkt i appen (till skillnad
          från Routes/Places API som går via backend) — kartritning sker på
          enheten. Nyckeln läses från local.properties (aldrig committad)
          via manifestPlaceholders i app/build.gradle.kts. Se KOM_IGANG.md
          för hur nyckeln skapas och begränsas till just den här appen.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@android:style/Theme.Material.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
